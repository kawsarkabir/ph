// 18-5 Run a loop for each element of an array
// target:: display every elements of an araay

let numbers = [45, 29, 90, 100, 200, 399]
for(let i = 0; i<numbers.length; i++){
    // console.log(i);
    let number = numbers[i]
    console.log(number);
}

